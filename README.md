# AI Workbench

Shared repository for the AI Workbench API and UI.

```
aiworkbench_api/   # FastAPI backend
aiworkbench_ui/    # React / Vite frontend
```

Use `aiworkbench_api/.env_sample` as a template for local API configuration. Do not commit `.env` files.
